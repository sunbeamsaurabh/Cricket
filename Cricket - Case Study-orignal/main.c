#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include"header.h"
int main()
{   
    
    prematch();
    toss_time();

    second_inning();
    after_match();
    // bat_score_card(T1);
    // bowling_score_card(T2);
    // bat_score_card(T2);
    // bowling_score_card(T1);


    return 0;
}